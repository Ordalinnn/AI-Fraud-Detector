{
  "name": "ai-fraud-detector-extension",
  "private": true,
  "description": "Browser extension for AI Fraud Detector — tests only, no build step needed to load the extension itself.",
  "scripts": {
    "test": "node --test tests/"
  }
}
